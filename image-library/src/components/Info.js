import React from 'react';
import './Info.css';

const Info = () => {
    // const { logos } = props
    // const result = logos.filter(info => info.id === logos.id)
    // console.log(result)
    return (
        <div>
            {/* {
                logos.map((card, index) => {
                    return(
                        <div id='modal' key={index}>
                            <h4>{`Team Name: ${card.team_name}`}</h4>
                            <h4>{`Conference: ${card.conference}`}</h4>
                            <h4>{`Division: ${card.division}`}</h4>
                        </div>
                    )
                })
            } */}
        </div>
    )
}

export default Info;
